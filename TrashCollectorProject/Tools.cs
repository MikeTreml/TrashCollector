using Geocoding;
using Geocoding.Google;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;


namespace TrashCollectorProject
{
    public static class Tools
    {
        //public static double LONGITUDE { get; set; }
        //public static double LATITUDE  { get; set; }

        //public static double async Task CordinateSearch(string address)
        //{
        //    double[] cordinates = { 0, 0 };

        //    IGeocoder geocoder = new GoogleGeocoder() { ApiKey = APIKeys.GOOGLE_API_KEY };
        //    IEnumerable<Address> addresses = await geocoder.GeocodeAsync(address);

        //    LATITUDE = addresses.First().Coordinates.Latitude;
        //    LONGITUDE = addresses.First().Coordinates.Longitude;

        //    return cordinates;
        //}
      
    }
}
